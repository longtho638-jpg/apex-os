# Phase 03: CRM Fix

- Priority: Medium
- Status: Completed (CRM Only)

## Overview
Fix `any` in CRM page. Provider List component moved to Mission 2.

## Related Code Files
- `src/app/[locale]/admin/crm/page.tsx` (Fixed)
- `src/app/[locale]/admin/providers/components/ProviderList.tsx` (Moved to Mission 2)

## Implementation Steps
1. Define `Pipeline` and `CRMEvent` interfaces for CRM page.
2. Define structure for `asset_config` and `regulatory_info` in `ProviderList.tsx`.

## Success Criteria
- No `any` in these 2 files.

# CLI TASK: PHASE 5 - CONTENT MARKETING (致人而不致於人)

## BINH PHÁP PRINCIPLE: 致人而不致於人 (Make Them Come to You)
**Translation**: "Control the enemy, don't let them control you"
**Application**: Create content that attracts users organically (SEO), so we don't chase them with ads.

---

## OBJECTIVE: 10x Organic Traffic via AI-Generated Content

**Target**: 
- Month 1: 500 visitors/month (from 50)
- Month 3: 5,000 visitors/month (10x growth)
- Month 6: 20,000 visitors/month (SEO compounding)

**Strategy**: AI writes ALL content (no manual writing) to save time and scale infinitely.

---

## TASK 1: AI Blog Automation

### 1.1 Blog Post Generator API
**File**: `src/app/api/admin/content/generate-post/route.ts`

```typescript
export async function POST(req: Request) {
  const { topic, keywords } = await req.json();
  
  // Use OpenRouter (DeepSeek-V3 for long-form content - $0.14/1M tokens)
  const prompt = `Write a 1500-word SEO-optimized blog post about "${topic}".
  Keywords: ${keywords.join(', ')}
  Include: Introduction, 3-5 sections, conclusion, FAQ.
  Tone: Professional but accessible.`;
  
  const response = await fetch('https://openrouter.ai/api/v1/chat/completions', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${process.env.OPENROUTER_API_KEY}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      model: 'deepseek/deepseek-chat',
      messages: [{ role: 'user', content: prompt }],
    }),
  });
  
  const data = await response.json();
  const content = data.choices[0].message.content;
  
  // Save to database (blog_posts table)
  // Return: { title, content, slug, seo_keywords, status: 'draft' }
}
```

**Database Migration**: `supabase/migrations/20251128_blog_posts.sql`
```sql
CREATE TABLE blog_posts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  title TEXT NOT NULL,
  slug TEXT UNIQUE NOT NULL,
  content TEXT NOT NULL,
  seo_keywords TEXT[],
  meta_description TEXT,
  status TEXT CHECK (status IN ('draft', 'published', 'archived')),
  views INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  published_at TIMESTAMPTZ
);

CREATE INDEX idx_blog_posts_slug ON blog_posts(slug);
CREATE INDEX idx_blog_posts_status ON blog_posts(status);
```

### 1.2 Blog Display Pages
**File**: `src/app/[locale]/blog/page.tsx` (List all posts)
**File**: `src/app/[locale]/blog/[slug]/page.tsx` (Single post)

**SEO Requirements**:
- Dynamic meta tags (title, description, keywords)
- Open Graph images (auto-generate with Vercel OG)
- Schema.org markup (Article type)
- Internal linking (auto-suggest related posts)

---

## TASK 2: Trading Calculators (Interactive Tools)

### 2.1 Profit Calculator
**File**: `src/app/[locale]/tools/profit-calculator/page.tsx`

**Features**:
- Input: Entry price, exit price, position size
- Output: Profit/loss, ROI %, risk/reward ratio
- CTA: "Get AI signals for better entries" → Signup

### 2.2 Position Size Calculator
**File**: `src/app/[locale]/tools/position-size/page.tsx`

**Features**:
- Input: Account size, risk %, stop loss distance
- Output: Recommended position size (BTC, contracts)
- CTA: "Let our AI manage risk for you" → Signup

### 2.3 DCA Calculator
**File**: `src/app/[locale]/tools/dca-calculator/page.tsx`

**Features**:
- Input: Investment amount, frequency, duration
- Output: Total invested, average entry price, final holdings
- CTA: "Automate DCA with our bot" → Signup

**共通要求 (Common Requirements)**:
- Mobile responsive
- Share button (Twitter, Telegram)
- Schema.org markup (SoftwareApplication)
- Internal link to signup

---

## TASK 3: SEO Infrastructure

### 3.1 Dynamic Sitemap
**File**: `src/app/sitemap.ts` (ENHANCE existing)

Add:
- All blog posts (`/blog/[slug]`)
- All tools (`/tools/profit-calculator`, etc.)
- Update frequency: Blog (weekly), Tools (monthly)

### 3.2 Structured Data
**File**: `src/components/seo/StructuredData.tsx`

```tsx
export function BlogPostStructuredData({ post }: { post: BlogPost }) {
  const schema = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    "headline": post.title,
    "datePublished": post.published_at,
    "author": { "@type": "Organization", "name": "Apex Rebate" },
    "keywords": post.seo_keywords.join(', '),
  };
  
  return <script type="application/ld+json">{JSON.stringify(schema)}</script>;
}
```

---

## TASK 4: Content Automation Cron

### 4.1 Auto-Generate Posts Daily
**File**: `src/app/api/cron/auto-content/route.ts`

**Logic**:
1. Fetch trending crypto topics from CoinGecko API
2. Generate 1 blog post per day (top trending coin)
3. Auto-publish if quality score > 8/10 (AI self-review)
4. Tweet summary to Twitter (if API connected)

**Frequency**: Daily at 6am UTC

### 4.2 SEO Performance Tracking
**File**: `src/app/api/admin/seo/performance/route.ts`

**Metrics**:
- Top 10 posts by views
- Average time on page
- Bounce rate by post
- Keyword rankings (manual Google Search Console integration)

---

## DELIVERABLES (Week 4 Day 1-2)

1. ✅ Blog system (generate + display)
2. ✅ 3 trading calculators
3. ✅ Enhanced sitemap + structured data
4. ✅ Daily content automation cron
5. ✅ SEO performance dashboard

---

## EXPECTED IMPACT

**Month 1**: 10 blog posts, 3 tools → 500 organic visitors
**Month 3**: 90 blog posts → 5,000 visitors (SEO compound effect)
**Cost**: ~$5/month in AI tokens (DeepSeek for long-form)

**兵法映射**:
- 致人: Content attracts users (they come to us)
- 不致於人: No ad spending (we don't chase them)

---

## BUILD REQUIREMENTS

- TypeScript strict mode ✅
- 0 technical debt ✅
- Mobile responsive ✅
- SEO-first design ✅
- Build: 0 errors ✅

**EXECUTE NOW VIA GEMINI CLI**
